# Um programa que leia duas notas de um
# aluno, calcule e mostre a sua média.
n1 = float(input('Digite a primeira nota: '))
n2 = float(input('Digite a segunda nota: '))
print('As notas {} e {} tem média de {}'.format(n1, n2, (n1 + n2) / 2))
