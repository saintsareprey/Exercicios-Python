# Escreva um programa que converta uma temperatura
# digitada em °C e converta para °F.
temp = float(input('Digite a temperatura em °C: '))
print('A temperatura {}°C convertida é de {}°F'.format(temp, temp * 1.8 + 32))
